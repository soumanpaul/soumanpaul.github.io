generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Views {
  slug      String   @id
  count     Int      @default(0)
  updatedAt DateTime @updatedAt
}
